[Unit]
Description=gasoline userspace VXLAN overlay
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/gasoline operator --config /etc/gasoline/config.yaml
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
